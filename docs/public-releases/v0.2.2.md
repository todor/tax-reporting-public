## Highlights

- SPB-8 manual-review guidance in the Bulgarian main report now points users to the generated helper CSV when missing start/end values need to be completed.
- Rerun instructions now document both supported flows: pass the completed file with `--spb8-input-file <path>` or place a completed file with `spb8` in its name in the input folder for auto-detection.
- No SPB-8 calculations, analyzer detection rules, appendix values, or tax classification logic changed in this release.
