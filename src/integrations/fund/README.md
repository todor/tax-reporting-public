# Fund Integrations

Fund integrations process fund-style event streams normalized to a shared fund IR:

- `deposit`
- `profit`
- `withdraw`

Current providers:

- `finexify`

Shared engine:

- `integrations.fund.shared`

Provider adapters:

- `integrations.fund.finexify`
